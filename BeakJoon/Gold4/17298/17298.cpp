#include <iostream>
#include <vector>

using namespace std;

int main()
{
    int N, i, x;
    cin >> N;

    vector<int> A;

    for (i = 0; i < A; i++)
    {
        cin >> x;
        A.push_back(x);
    }

    
}