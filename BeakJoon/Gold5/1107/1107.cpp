#include <iostream>
#include <vector>

using namespace std;

int main()
{
    int N, M, i, x, cnt = 0;
    int channel = 100;
    vector<int> doo;

    cin >> N;
    cin >> M;

    for (i = 0; i < M; i++)
    {
        cin >> x;
        doo.push_back(x);
    }

    for(i=0;i<M;i++)

    while (channel != N)
    {

        cnt++;
    }

    cout << cnt;
}