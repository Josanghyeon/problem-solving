#include <iostream>
#include <vector>

using namespace std;

int main()
{
    int N, K;
    cin >> N >> K;

    vector<pair<int, int>> n(N);
    for (int i = 0; i < N; i++)
    {
        cin >> n[i].first >> n[i].second;
    }

    for (int i = 0; i < K; i++)
    {
        
    }
}